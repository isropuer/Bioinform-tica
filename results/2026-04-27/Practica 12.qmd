---
title: "PRÁCTICA 12"
author: "Ismael Rodrigo Puerto"
format:
  html:
    embed-resources: true
engine: knitr
editor: visual
---

## Preparación
Sólo necesitas instalar el paquete DESeq2 de Bioconductor. Si lo haces en el ordenador virtual y tienes problemas de compatibilidad entre la versión de R y la de Bioconductor, sigue la estrategia siguiente:

  1. Crea una carpeta R en tu directorio de entrada al ordenador.
  2. Inicia una sesión de R y añade el directorio al .libPaths, así: .libPaths('./R') (suponiendo que tu directorio de trabajo es el mismo donde has creado la carpeta R).
  3. Actualiza la versión de BiocManager a la 3.22 de la siguiente manera (atención, esto lleva un par de horas): BiocManager::install(version = '3.22', lib = './R').
  4. Instala el paquete DESeq2: BiocManager::install('DESeq2', lib = './R').

## Datos
Utilizaremos las medidas de expresión génica del archivo salmon.merged.gene_counts.tsv que podéis descargar en este enlace o en el aula virtual. Los datos proceden de 12 genotecas de RNA-seq de lecturas emparejadas, de muestras de Drosophila melanogaster de dos orígenes geográficos (Panamá y Maine) y dos tratamientos de temperatura (low y high) (Zhao 2015). Hay 3 réplicas biológicas para cada combinación de región y temperatura. Si tenéis curiosidad sobre cómo se han obtenido los recuentos de lecturas por gen a partir de los archivos FASTQ, podéis consultarlo en este enlace.

Además, necesitáis el archivo dme_elev_samples.tsv, una tabla que indica a qué región y tratamiento pertenece cada muestra. Lo podéis descargar de este enlace o del aula virtual.

Los dos archivos deben estar guardados en la carpeta de datos.

## Exploración de los datos
```{r}
muestras <- read.table('../../data/dme_elev_samples.tsv', header = TRUE)
genex <- read.table('../../data/salmon.merged.gene_counts.tsv',
                    header = TRUE)
```

**Ejercicio 1**
**Recuerda dejar constancia en un bloque de código de cómo obtienes las respuestas.**

**Usa la función paste() para añadir una columna a muestras que contenga la población y la temperatura unidas por _.**
```{r}
muestras <- cbind(muestras, population_temp = paste(muestras$population, muestras$temp, sep = "_"))
```

**¿Cuál es el número total de lecturas por muestra?**
```{r}
sum(genex$SRR1576457)
sum(genex$SRR1576458)
sum(genex$SRR1576459)
sum(genex$SRR1576460)
sum(genex$SRR1576461)
sum(genex$SRR1576462)
sum(genex$SRR1576463)
sum(genex$SRR1576464)
sum(genex$SRR1576465)
sum(genex$SRR1576514)
sum(genex$SRR1576515)
sum(genex$SRR1576516)
```

**¿Los recuentos tienen distribuciones parecidas entre muestras?**
```{r}
f <- rowSums(genex[,-c(1,2)])>0
plot(density(genex[f ,3]), log = 'x', xlim= c(1,10000))
lines(density(genex[f ,4]))
lines(density(genex[f ,5]))
lines(density(genex[f ,6]))
lines(density(genex[f ,7]))
lines(density(genex[f ,8]))
lines(density(genex[f ,9]))
lines(density(genex[f ,10]))
lines(density(genex[f ,11]))
lines(density(genex[f ,12]))
lines(density(genex[f ,13]))
lines(density(genex[f ,14]))
```
La mayoría de las lecturas se agrupan en el mismo rango de valores, por lo que las muestras tienen distribuciones similares
**¿Cuántos genes no se expresan en absoluto en ninguna de las muestras?**
```{r}
# Cuenta cuántos genes tienen una suma total de cero en todas las columnas. Como las dos primeras columnas no son numéricas, las tenemos que quitar, si no la función no va a saber sumar.
sum(rowSums(genex[, -c(1,2)]) == 0)
```
Por tanto, hay 4722 genes que no se expresan en ninguna de las muestras

## Crear el objeto de clase DESeqDataSet
Para crear un objeto de clase DESeqDataSet necesitamos que las filas de la tabla muestras estén en el mismo orden que las columnas en genex. La función DESeqDataSetFromMatrix() necesita que los recuentos sean una matriz, no un marco de datos. Y en muestras debe haber factores.
```{r}
#Comprobación de que los nombre de las columnas 3 a 14 de genex sean los mismos que los de la columna sample de muestras, y que estén en el mismo orden. TRUE indica que sí. La función stopifnot() hace que si no es correcto se bloquee
suppressMessages(library('DESeq2'))
all(names(genex)[3:14] == muestras$sample)
```

```{r}
# Pasamos la columna "sample" a nombres de fila:
row.names(muestras) <- muestras$sample
# Eliminamos la columna "sample":
muestras$sample <- NULL
# Y convertimos "population" y "temp" en factores: variables categóricas que solo admiten un nº concreto de valores posibles (niveles). Los factores pueden tener los niveles ordenados.
muestras$population <- factor(muestras$population,
                              levels = c('maine', 'panama'))
muestras$temp       <- factor(muestras$temp,
                              levels = c('low', 'high'))
muestras
```

Aunque no hacía falta definir los niveles de los factores, lo indico porque es un buen momento para definir manualmente su orden (por defecto se ordenan alfabéticamente). El orden de los niveles determina qué nivel actúa como referencia en los contrastes estadísticos (el primero). Esto resultaría importante, por ejemplo, al comparar tratamientos respecto de un control.

Los datos de recuento deben ser números enteros y en forma de matriz
```{r}
row.names(genex) <- genex$gene_name
genex$gene_name <- NULL
genex$gene_id   <- NULL
genex <- round(as.matrix(genex))
# Como hemos eliminado gene_name y gene_id, ya todo lo que queda es numérico y por consiguente ya podemos usar esta función
```

```{r}
# Creamos un objeto dataseq a partir de una matriz
dds1 <- DESeqDataSetFromMatrix(
   countData = genex,
   colData = muestras, # datos de las columnas (las muestras)
   design = ~ population + temp # Diseño estadístico. "~" indica que a continuación van una serie de variables que pueden afectar a los datos
)
dds1
```

**Note**
*Los objetos DESeqDataSet pueden contener información sobre las coordenadas cromosómicas de los genes o tránscritos, si se construyen a partir de otro tipo de datos.*

## Pre-filtrado
Una fracción importante de genes no se están expresando en ningúna muestra. Aunque el filtrado no es necesario, ayuda a reducir la memoria ocupada por dds1:
```{r}
keep <- rowSums(counts(dds1)) > 0 #Tengo que usar counts() para acceder a la matriz, indica que diga que filas la suma da  >0. Que de 0 significa que ninguna muestra tiene ese gen, por lo que se eliminan y así reducimos el tamaño de la variable dds1
dds1 <- dds1[keep, ]
```

## Expresión diferencial
Observa que la función DESeq() produce un objeto también de clase DESeqDataSet, pero con resultados añadidos:
```{r}
dds1 <- DESeq(dds1)
```

```{r}
res <- results(dds1)
res
```
base mean: el valor base de cada gen
log2foldchange: Cuantas veces + expresado está un gen en temperatura alta que en baja (si está en positivo es que está más a temp alta, y si es negativo en baja). log2 de la expresión en condición X entre la expresión de referencia
lfcSE: el error estandar de log2
stat: estadístico
p_value: p valor
padj: P valor ajustado
Por defecto, la función results() produce la tabla de resultados para el contraste entre el nivel último y el de referencia del último factor en la tabla que describe las muestras. En este caso, el factor es temp, el nivel de referencia es low y el otro nivel es high. Por tanto, los valores de log2FoldChange representan el logaritmo en base 2 de la relación entre niveles de expresión en muestras high respecto de muestras low.

**Ejercicio 3**
**Mira la ayuda de results() y genera también la tabla de resultados para el contraste entre las dos regiones de procedencia de las moscas.**
Es importante generar contraste para poder interpretar bien los resultados.
```{r}
res2 <- results(dds1, contrast = c("population", "maine", "panama"))
res2
```
En este caso los valores del log2 expresan la relación entre la expresión en maine y en panama, por lo que si el valor es positivo es que se expresa más en maine, y si es negativo se expresa más en panama.
**Usa summary() sobre los resultados para ver el número de genes diferencialmente expresados entre cada par de condiciones.**
```{r}
summary(res)
summary(res2)
```
Hay 2418 genes sobreexpresados en temperatura alta y 2336 en temperatura baja. Hay 634 genes sobreexpresados en maine y 623 sobreexpresados en panama
**¿Cuál es la tasa de falsos positivos (FDR)?**
```{r}
FDR1 <- sum(res$padj < 0.1, na.rm = TRUE)
(FDR1/19508)/(1-FDR1/19508)
FDR2 <- sum(res2$padj < 0.1, na.rm = TRUE)
(FDR2/19508)/(1-FDR2/19508)
# La función suma todos los valores de padj menores a 0,1
```


## Gráficas
```{r}
plotMA(res)
```

Los genes con niveles de expresión más bajos producen resultados más ruidosos (la varianza es mayor). La función lfcShrink() contrae (shrink) las estimaciones de los efectos (log fold change) para reducir el ruido y hacer el resultado más realista:

```{r}
resShrink <- lfcShrink(dds1, coef = 'temp_high_vs_low',
                       type = 'normal')
plotMA(resShrink)
```

**Ejercicio 4**
**¿Sabrías hacer una gráfica volcán?**

## Extraer datos transformados
Aunque los datos de entrada no deben estar normalizados y la función DESeq() ya tiene en cuenta las diferencias de tamaño de genoteca entre muestras y la relación entre media y varianza, puede ser útil disponer de los datos normalizados para representarlos gráficamente. Para ello podemos usar las funciones vst() y assay():
